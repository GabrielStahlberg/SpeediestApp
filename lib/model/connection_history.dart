import 'package:flutter/material.dart';

class ConnectionHistory {
  final id;
  final name;
  final location;
  final duration;
  final average;
  final date;

  ConnectionHistory(this.id, this.name, this.location, this.duration, this.average, this.date);
}